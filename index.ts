const travis = 75
const gorman = 25
const addem = travis + gorman

console.log( addem )

const phrase = `once upon a time, I had $${travis}. But then I got $${gorman} more. So, now I have $${addem}`

console.log( phrase )